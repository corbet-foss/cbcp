[package]
name = "cbcp"
version = "0.1.1"
entrypoint = "typst/cbcp.typ"
authors = ["Julian Y. Richard Corbet"]
license = "LGPL-3.0-only WITH LGPL-3.0-linking-exception"
description = "Deterministic BCP 47 locale-ID casting and vendor code mapping. Mirrors the cbcp Rust crate release line."
repository = "https://github.com/corbet-foss/cbcp"
exclude = [".git", ".github", ".crow", "target", "dist", "js", "py", "tests", "scripts"]
